require "spec_helper"

describe ComicReleases do
  it "has a version number" do
    expect(ComicReleases::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
